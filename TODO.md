# TODO — План развития проекта

## Приоритет 1: Расширение поддержки операторов

### Математические функции
- [ ] `arccos`, `arctan`, `arccot`
- [ ] `arcsinh`, `arccosh`, `arctanh` (обратные гиперболические)
- [ ] `coth`, `sech`, `csch` (гиперболические котангенс и др.)
- [ ] `sec`, `csc` (секанс и косеканс)
- [ ] `floor`, `ceil` (округление)
- [ ] `abs` (модуль/абсолютное значение)
- [ ] `exp` (экспонента)
- [ ] `ln` (натуральный логарифм)

### Операторы сравнения
- [ ] `≠` (не равно) — `neq`
- [ ] `≤` (меньше или равно) — `leq`
- [ ] `≥` (больше или равно) — `geq`
- [ ] `∼` (подобно/асимптотически) — `sim`
- [ ] `≡` (тождественно равно) — `equiv`
- [ ] `∝` (пропорционально) — `propto`

### Матрицы и определители
- [ ] `матрица` — `\begin{matrix} ... \end{matrix}`
- [ ] `определитель` — `\det`
- [ ] `транспонированная` — `^T`

### Дроби
- [ ] `дробь из а на б` → `\frac{a}{b}`
- [ ] `а делить на б` → `a / b`

### Производные
- [ ] `производная от икс` → `\frac{d}{dx}`
- [ ] `вторую производную` → `\frac{d^2}{dx^2}`
- [ ] `частную производную` → `\partial`

### Суммы и произведения
- [ ] `сумма от ноль до бесконечности` → `\sum_{n=0}^{\infty}`
- [ ] `произведение от один до n` → `\prod_{i=1}^{n}`

### Интегралы (приоритет)
- [ ] `интеграл от нуля до единицы` — исправить текущую реализацию
- [ ] `кратный интеграл` — `\iint`, `\iiint`
- [ ] `криволинейный интеграл` — `\oint`

### Специальные символы
- [ ] `бесконечность` → `\infty`
- [ ] `пустое множество` → `\emptyset`
- [ ] `принадлежит` → `\in`
- [ ] `не принадлежит` → `\notin`
- [ ] `подмножество` → `\subset`
- [ ] `объединение` → `\cup`
- [ ] `пересечение` → `\cap`

## Приоритет 2: Естественные тесты

### Сбор датасета
- [ ] Извлечь 100+ реальных примеров из учебников
- [ ] Добавить примеры из Википедии (русская версия)
- [ ] Взять задачи из сборников по математике

### Формат тестов
```jsonl
{
  "input": "пусть икс равно синус от тета",
  "normalized": "x = sin(theta)",
  "latex": "x = \\sin(\\theta)"
}
```

### Категории тестов
1. **Алгебра** — уравнения, неравенства
2. **Матанализ** — пределы, производные, интегралы
3. **Геометрия** — формулы площадей, объёмов
4. **Тригонометрия** — тождества, уравнения
5. **Комбинаторика** — биномы, факториалы

## Приоритет 3: Улучшение нормализатора

### Обработка контекста
- [ ] Различать "и" (союз) и "и" (пересечение множеств → `\cap`)
- [ ] "от" в интеграле vs "от" (дистанция)
- [ ] "на" (деление) vs "на" (направление)

### Расширение словарей
- [ ] Добавить больше греческих букв
- [ ] Обработать вариации ("лямбда" / "лямбда заглавная")
- [ ] Сокращения ("син" → `sin`, "кос" → `cos`)

### Ошибки нормализатора (известные)
- [ ] Кириллические буквы в именах переменных не заменяются
- [ ] "с" в "синус" может конфликтовать с предлогом "с"

## Приоритет 4: Интеграция и CLI

### CLI интерфейс
```bash
python -m larktex "синус от икс"
# Вывод: \sin(x)

python -m larktex --file input.txt --output output.tex
```

### Python package
- [ ] `pip install larktex`
- [ ] `import larktex; larktex.convert("...")`

### Документация
- [ ] Полное руководство пользователя
- [ ] Примеры для каждой категории математики
- [ ] API документация

## Приоритет 5: Производительность

- [ ] Кэширование результатов нормализации
- [ ] Оптимизация словарей (trie для быстрого поиска)
- [ ] Батч-обработка больших файлов

---

## Статус на сегодня (26 апреля 2026)

### ✅ Завершённые задачи

**Греческие буквы в LaTeX:**
- Нормализатор теперь выдаёт `\theta` вместо `θ`
- Grammar обновлён для распознавания `\alpha`, `\theta` и т.д.
- Parser корректно обрабатывает `\theta`, `\alpha` и другие греческие буквы

**Форматирование функций:**
- `cos(x)` — без пробела перед скобкой
- `cos^2(x)` — функция со степенью: `function^N(arg)`
- `\cos^2 \theta` — греческие буквы без скобок: `function^N \theta`

**Удаление `\cdot`:**
- Убран из умножения в parser.py

**Тесты:**
- `tests/script_test.py` — тестовый runner с поддержкой `--file`
- Первый тест из `wiki_script.jsonl` теперь проходит:
  - `косинус два тета равно косинус квадрат тета минус синус квадрат тета`
  - → `\cos(2\theta) = \cos^2 \theta - \sin^2 \theta`

**Design.py:**
- `reduce_nested_parens()` убирает лишние скобки: `(a + b)` → `a + b`
- `add_latex_escapes()` добавляет `\` к функциям

### ⚠️ Известные проблемы

1. **Первый тест пройден, остальные падают**
   - Тест 2: "предел при икс стремящемся к бесконечности..." — ошибка парсера
   - Слова "предел", "экспонента", "бесконечность" не поддерживаются

2. **Поддержка новых функций:**
   - `предел` → `\lim`
   - `экспонента` → `\exp`
   - `бесконечность` → `\infty`
   - `стремится к` → `→`

3. **Лишние скобки:**
   - Parser всё ещё добавляет скобки вокруг групп выражений
   - Пример: `(a + b)` вместо `a + b` в некоторых случаях

4. **SyntaxWarning в design.py:**
   - `invalid escape sequence '\s'` в docstring

### 📋 План дальнейших действий

1. **Добавить поддержку математических терминов:**
   - `предел` → `limit`
   - `бесконечность` → `inf`
   - `экспонента` → `exp`

2. **Расширить grammar.lark:**
   - Добавить правила для `limit`, `inf`, `exp`

3. **Доработать normalizer.py:**
   - Добавить синонимы для новых терминов

4. **Исправить SyntaxWarning:**
   - Использовать сырые строки в docstring

5. **Продолжить тестирование:**
   - Запустить все 62 теста из `wiki_script.jsonl`
   - Исправить ошибки по мере их появления

6. **Улучшить обработку скобок:**
   - Убедиться, что `design.reduce_nested_parens()` работает корректно
   - Убрать лишние скобки из output parser

**Время:** 26 апреля 2026
**Следующее занятие:** Добавить поддержку "предел", "бесконечность", "экспонента" и продолжить тестирование

Удачи! 🚀