# Нормализатор математического текста

## Описание

Нормализатор преобразует русский текст с математическими выражениями в латиницу. Это промежуточный этап перед парсингом в LaTeX.

## Архитектура

```
Русский текст → normalizer.py → Латиница → grammar.lark → parser.py → LaTeX
```

## Основные шаги обработки

### 1. Загрузка словарей

При инициализации нормализатор загружает один JSON-файл `i18n/ru.json`:

- **variables** — переменные (икс, игрек, зет..., A, B, C...)
- **functions** — функции (синус, косинус, тангенс..., sqrt, log...)
- **operators** — операторы (плюс, минус, умножить, делить...)
- **powers** — степени (в квадрате, в кубе...)
- **numbers** — числа (ноль, один, два...)
- **special** — константы (пи, эйлера...)
- **relations** — отношения (приблизительно, эквивалентно...)
- **integrals** — интегралы (интеграл, двойной интеграл...)
- **logic** — логика (для любого, существует...)
- **misc** — прочее (всё, скобки...)

### 2. Построение обратного отображения

Создаётся словарь для быстрого поиска: каждое слово → его нормальная форма.

Пример:
- "икс" → "x"
- "синус" → "sin"
- "альфа" → "α"

### 3. Обработка степеней (первый проход)

Сначала заменяются фразы со "в":
- "в квадрате" → "^2"
- "в кубе" → "^3"

Это делается до удаления предлога "в".

### 4. Обработка "всё"

" всё " ( все) заменяется на all:
- "а плюс б всё делить на в" → "a + b all / в"

### 5. Обработка деления

"делить на" заменяется на " / ":
- "делить на" → " / "

### 6. Удаление предлогов

Удаляются математически несущественные предлоги:
- "от"
- "из"
- "на"
- "до"
- "в "

Пример: "интеграл от икс до игрек" → "integral икс игрек"

### 7. Обработка скобок

- "открыть скобку" → "("
- "закрыть скобку" → ")"
- "открыть" → "("
- "закрыть" → ")"

### 8. Замена синонимов на латиницу

Основной этап. Слова заменяются по словарю в порядке убывания длины (сначала длинные фразы):

1. Сортируем все синонимы по длине (от длинных к коротким)
2. Для каждого синонима ищем в тексте и заменяем на нормальную форму
3. Пропускаем уже обработанные слова ("открыть скобку", "всё", предлоги...)
4. Пропускаем однобуквенные латинские символы (чтобы не менять x → X)

Примеры:
- "синус" → "sin"
- "икс" → "x"
- "альфа" → "α"
- "дэ заглавная" → "D"

### 9. Очистка пробелов

- Несколько пробелов → один
- Пробел перед "^" удаляется ("x ^2" → "x^2")
- Пробел после "(" удаляется ("( x" → "(x")
- Пробел перед ")" удаляется ("y )" → "y)")
- **Пробелы вокруг операторов сохраняются**: "a + b / c" (не "a+b/c")

Примеры:
- "a + b / c" остаётся как "a + b / c"
- "x ^2" → "x^2"
- "( x + y )" → "(x + y)"

## Примеры работы

| Вход | Выход |
|------|-------|
| "синус от икс" | "sin x" |
| "икс равно игрек" | "x = y" |
| "корень из а плюс б" | "sqrt a + b" |
| "икс в квадрате" | "x^2" |
| "интеграл икс де икс" | "integral x d x" |
| "дэ заглавная" | "D" |
| "мю равно хи" | "μ = χ" |
| "тэ заглавная приблизительно эй" | "T approx E" |
| "а плюс б всё делить на в" | "a + b all / v" |

## Обработка заглавных букв

- **Строчные**: "икс" → "x", "ю" → "u"
- **Заглавные**: "икс заглавная" → "X", "ю заглавная" → "U"
- **Синонимы**: "оу" → "U", "эй" → "E", "би" → "B"

## Обработка греческих букв

Все греческие буквы заменяются на символы Unicode:
- "альфа" → "α"
- "бета" → "β"
- "дельта" → "δ"
- "мю" → "μ"
- "хи" → "χ"
- "эта/эт" → "η"

## Интерфейс

```python
from normalizer import Normalizer

normalizer = Normalizer(i18n_dir="i18n")

# Обработка одной строки
result = normalizer.normalize_text("синус от икс")
# Результат: "sin x"

# Полная обработка (с выделением островов)
result = normalizer.process("пусть икс равно синус от у")
# Результат: "x = sin u"

# Выделение математических островов
islands = normalizer.extract_math_islands("пусть икс равно синус от у")
# Результат: [("пусть ", False), ("икс равно синус от у", True)]
```

## Тестирование

### Unit тесты
```bash
uv run pytest tests/test_normalizer.py -v
```

### Тестирование на реальных данных
```bash
uv run python tests/verify_normalizer.py
```

Используется файл `tests/expected_outputs.jsonl` с эталонными результатами.

## Ограничения

1. **Нормализатор не создаёт LaTeX** — только переводит в латиницу
2. **Нет проверки контекста** — "ю" всегда → "u" (строчная), "ю заглавная" → "U"
3. **Некоторые слова остаются** — "ипсилон", "ди" не всегда заменяются
4. **Падежи упрощены** — все падежные формы ведут к одной базовой
5. **"всё" → "("** — не закрывает скобку автоматически

## Файлы проекта

- `normalizer.py` — основной модуль нормализатора
- `i18n/ru.json` — словарь синонимов
- `tests/expected_outputs.jsonl` — тестовые данные
- `tests/verify_normalizer.py` — скрипт верификации
- `tests/test_normalizer.py` — unit тесты
