[project]
name = "larktex"
version = "0.1.0"
description = "Math text to LaTeX converter"
requires-python = ">=3.10"
dependencies = [
    "lark>=1.1.9",
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
